# AGENTS.md

## Project Context

This is the YBS Coaching OS repository migrated to **Supabase**.
Treat it as user-owned application code, keep changes focused on the user's request, and preserve existing project conventions.

## Architecture

- **Database & Auth**: Supabase PostgreSQL with Row Level Security (RLS) and Supabase Auth.
- **Frontend**: React + Vite using Tailwind CSS and Radix UI primitives.
- **Service Layer**: Centralized Supabase data-access services in `src/services/` (workspaces, clients, subscriptions, packages, assessments, metrics, nutrition, workouts, foods, exercises, team, notifications, audit, storage).
- **Authentication & Roles**: Strictly driven by trusted Supabase `profiles` and `workspace_memberships` rows through `AuthContext.jsx` and `ybs-auth.js`.
- **File Storage**: Private Supabase Storage bucket for client progress photos with time-limited signed URLs (`src/services/storage.js`).

## Key Files

- `src/`: frontend application source.
- `src/utils/supabase.ts`: Supabase client initialization.
- `src/services/`: Supabase data-access and storage services.
- `src/lib/AuthContext.jsx`: canonical auth session provider.
- `vite.config.js`: standard Vite React configuration.
- `.env.local`: local environment variables (`VITE_SUPABASE_URL`, `VITE_SUPABASE_PUBLISHABLE_KEY`).

## Working Notes

- Use `npm run dev` for local development.
- Build verification: `npm run build`.
- **Phase 4 Form Automation is APPLIED to the remote project.** Migration `20260910000004_form_automation_rules.sql` creates `public.form_assignment_rules` + `public.form_assignment_instances`, the SECURITY DEFINER RPCs (`create_form_assignment_rule`, `update_form_assignment_rule`, `set_form_assignment_rule_enabled`, `delete_form_assignment_rule`, `renew_subscription`), replaces the hardcoded master-intake block inside `public.on_client_application_approved()` with `evaluate_form_rules_for_approval`, seeds the default **Your First Step** rule, adds the nutrition/workout combined trigger + `workout_plans` insert/update trigger, and schedules the pg_cron weekly/biweekly sweeper. The migration was applied manually and repaired into the migration history; full remote verification (round 2) passed. Note: `renew_subscription` is the new Trigger-5 event path — renewal form rules fire only through this RPC.
- Zero Base44 runtime dependencies (`__B44_DB__`, `db.auth`, `db.entities` are completely removed).
- **Pending Supabase migrations must be applied to the remote project before the app works fully.** Migrations in `supabase/migrations/` that are not yet pushed/applied to the remote project (`sakvtstauikdrlthhlij`): `20260904000007_workspace_partnership_capacity.sql` (creates `public.partnership_types`, adds `workspaces.partnership_type_id`, capacity/RPC/triggers — until applied, the Create Workspace **Partnership Type** dropdown stays empty), `20260904000008_workout_programming_tracking.sql`, and `20260908000006_meal_replacement_requests.sql` (meal replacement request flow: table + RLS + notify trigger + `approve_meal_replacement_request`/`reject_meal_replacement_request` RPCs — until applied, the client **Replace** button/request flow and the staff **Replacement Requests** page will error), plus `20260908000007_fix_meal_replacement_insert_rls.sql` (repairs the `meal_req_insert` policy so a legitimate client request is not rejected by a workspace-id payload equality — replace the old `meal_req_insert` policy in your SQL editor even if `20260908000006` is already applied), and `20260908000008_assessment_responses_question_id_no_fk.sql` (drops the `assessment_responses.question_id` FK so client Save Progress works for forms whose snapshot question IDs were orphaned when template questions were removed/recreated — until applied, the client **Save Progress** button shows "Failed to save" on affected existing forms). Apply via `supabase db push` (or run the SQL in the Supabase SQL editor) after committing these files.
- **External team invitations (edge functions + Activate).** `generate-trainer-invite` and `generate-owner-invite` now follow the external-invite model: `invite_team_member` mints `platform_invites.token`, the functions return `/activate?token=<token>` URLs, and no Auth user is created at generation time. This depends on migration `20260907000004_team_invitation_tokens.sql` (token column, `get_team_invite` RPC, trainer-membership trigger) being applied, AND both functions being redeployed: `supabase functions deploy generate-trainer-invite` + `generate-owner-invite`. Until then the functions still run the old GoTrue `listUsers`/`generateLink` logic (which can fail with `lookup_failed`).
- **Phase 5 Structured Package Features.** Migration `20260910000005_package_features.sql` adds `public.package_features` (id/package_id/title/sort_order/is_active; RLS SELECT-only mirroring `packages_select`, no write policies), backfills rows from the legacy `packages.features` TEXT[] projection, adds `subscriptions.features` snapshot column + INSERT-only `snapshot_subscription_features` trigger + historical backfill, adds the SECURITY DEFINER `sync_package_features(p_package_id, p_items jsonb)` RPC as the ONLY features write path (owner-scope check, upsert/soft-delete reconcile, rebuilds `packages.features` + `packages.updated_at`, one aggregated `package_features_updated` audit row per real diff), and redefines `clone_default_packages_for_workspace` to clone feature rows into workspace packages. `packages.features` remains a read-only projection; the editor (`src/pages/Packages.jsx` FeaturesEditor) persists rename/add/delete/reorder through `PackagesService.listFeatures`/`saveFeatures` (RPC), while scalar edits keep using `PackagesService.update`. Historical subscription snapshots are immutable — editing a package never rewrites past `subscriptions.features`. Apply via `supabase db push` (or run the SQL in the Supabase SQL editor) before testing the Features editor; until applied, editing features on the Packages page shows "Failed to save" and new subscriptions get no snapshot.
- **Phase 6 Exercise Planner Enhancements (Session Ordering, Duplicate, Rest Day Items).** Migration `20260911000001_workout_session_ordering_and_rest_days.sql` adds `day_type TEXT NOT NULL DEFAULT 'session' CHECK (day_type IN ('session', 'rest_day'))` to `public.workout_days`, backfills existing rest day rows, establishes bi-directional sync trigger `trg_sync_workout_day_type` between `day_type` and `rest_day` boolean for full backward compatibility, and adds composite index `idx_workout_days_plan_sort` on `(workout_plan_id, sort_order ASC)`. In the application layer, `WorkoutsService` supports resilient insert fallback, session drag & drop reordering (`@hello-pangea/dnd`), deep-copy duplication with deterministic naming ("Upper 1" -> "Upper 2"), and first-class Rest Day items with dedicated instructions modal and recovery view. Apply via `supabase db push` or SQL editor.
- **Activation-anchored recurring forms (NUMBER vs NUMBER RANGE inputs).** Migration `20260916000001_activation_anchored_recurring_forms.sql` redefines `public.assign_form_for_rule` so weekly/biweekly check-ins anchor to `clients.activated_at` (cycle N due = activation date + N × `recurrence_days`; only the current due cycle is generated — no missed-cycle back-fill; per-cycle deterministic dedup key `'r:<rule>:c:<client>:d:<due YYYY-MM-DD>'` keeps sweeps idempotent; unactivated clients are a clean no-op) and tightens `evaluate_due_recurring_form_rules()` to only sweep active, `activated_at`-set clients. No tables/columns/constraints change. **NUMBER vs NUMBER RANGE** reuses the existing `assessment_questions.conditional_rules` JSONB (already frozen into `questions_snapshot`) via a `numeric_accept: 'number_range'` marker — no schema change. `FormBuilder.jsx` shows a "Single Number / Number Range" selector for `number` questions, now passes `conditional_rules` through on save (also restores section preservation), and `FormFiller.jsx` renders NUMBER as `type="number"` but NUMBER RANGE as a text input with Arabic inline validation (`هذا السؤال مطلوب.` / `من فضلك أدخل رقمًا واحدًا فقط، مثال: 5000.` / `من فضلك أدخل النطاق بالشكل: 7.5–9.` / `من فضلك أدخل قيمة رقمية صحيحة.`; accepts `-`, `–`, `—` separators with optional spaces; ranges are stored as text-capable strings in `assessment_responses.response_value`, never collapsed to a scalar). Apply via `supabase db push` or SQL editor before relying on the new cadence (until then the executor still anchors to last assignment).
- **One-time cleanup: pre-activation-anchored recurring forms.** Migration `20260917000001_one_time_cleanup_pre_activation_recurring_forms.sql` deletes only recurring (weekly/biweekly) `form_assignment_instances`/`assessments` generated by the OLD "last-assignment" scheduler that are still awaiting response: ledger `status='assigned'` AND `submission_status='pending'` AND `submitted_at IS NULL` AND zero `assessment_responses` AND `assessments.due_date > form_assignment_instances.assigned_at::date` (provable wrong-time signature — old logic always set `due_date = assigned_at + interval`, new logic always sets `due_date = activation + cycle*interval <= assigned_at`). Deletes only the `assessments` rows (cascades responses + ledger + `ai_analysis_cache`); templates, rules, one-off triggers, submitted/reviewed history, response-bearing rows, and all new-logic instances are untouched; the activation-anchored scheduler and its dedup are not redefined. **Run `ybs-cleanup-dryrun.sql` in the Supabase SQL editor (owner role) FIRST and review the `safe_to_delete` rows before applying.**
- **Phase 8 Member Display Names (First/Last + Team edit).** Migration `20260918000002_member_display_names.sql` adds nullable `profiles.first_name`/`profiles.last_name` and the SECURITY DEFINER RPC `update_member_display_name(p_user_id, p_first_name, p_last_name)` as the ONLY path to change another member's name (client `profiles_update` RLS stays self/platform-owner locked; gate = platform owner OR workspace owner of a workspace where the target holds an active staff membership; trims names, rejects whitespace-only/empty, 80-char cap, keeps `full_name` in sync, preserves email/role/memberships, appends a `member_name_updated` audit row). Frontend: `Activate.jsx` requires First/Last on the invite path (sent to the edge fn, enforced server-side), `activate-invite` persists `first_name`/`last_name`/`full_name = "<first> <last>"` on both new (`createUser` metadata + `ensureProfile`) and existing-account paths, `Team.jsx` gets an Edit actions column (gated `canManageTeam`) opening a name modal via `TeamService.updateMemberName`, and `memberDisplayName()` (`src/lib/ybs-utils.js`) is the shared display helper. Apply ONLY this file via `supabase db query --linked -f supabase/migrations/20260918000002_member_display_names.sql` (never a bare `supabase db push` — it would also run the forbidden `20260917000001` cleanup) and redeploy the edge fn: `supabase functions deploy activate-invite`.
- **Phase 7 Individual Form-Instance Deletion (Admin → Forms).** Migration `20260918000001_delete_assessment_instance.sql` (APPLIED to the remote project) reworks the ledger link `form_assignment_instances.assessment_id` from `NOT NULL … ON DELETE CASCADE` to **nullable + ON DELETE SET NULL** and adds the SECURITY DEFINER RPC `delete_assessment_instance(p_assessment_id UUID)` as the ONLY delete path (mirrors `assessments_delete` RLS: platform owner or workspace owner only; coaches excluded). One transaction deletes notifications tied to the assessment and the assessment row itself (cascading `assessment_responses` + `ai_analysis_cache`), keeps the ledger row intact (dedup_key/next_due_at preserved → a deleted recurring cycle can NEVER regenerate at the next cron sweep; the next valid cycle still creates a fresh instance), handles ledger-less legacy instances, appends a `form_instance_deleted` audit row, and never touches the template/questions/rules/scheduler. Frontend: `AssessmentsService.deleteInstance` (`src/services/assessments.js`) + Trash2 row action and confirmation modal in `src/pages/Assessments.jsx` (button gated by `forms.delete`; success toast "Form deleted successfully."). NOTE: never run a bare `supabase db push` here — it would also apply the forbidden cleanup migration `20260917000001`; apply only this file via `supabase db query --linked -f supabase/migrations/20260918000001_delete_assessment_instance.sql` (or the SQL editor).
- **Registration link service-role grants (APPLIED).** Migration `20260919000002_registration_link_service_role_grants.sql` (APPLIED to the remote project) adds `GRANT EXECUTE … TO service_role` on the five registration-link RPCs (`resolve_registration_link(text)`, `resolve_phone_identifier(text)`, `reserve_registration_link_ip(uuid, inet)`, `finalize_registration_reservation(uuid, inet, uuid)`, `release_registration_link_ip(uuid, inet)`). These were created with only `anon, authenticated` grants by prior migrations; the `client-registration` Edge Function authenticates as `service_role` via SUPABASE_SERVICE_ROLE_KEY, so every call failed with `link_unavailable` 500. Applied via `supabase db query --linked -f`; apply the same file on fresh environments. Frontend hardening: `src/services/registrationLinks.js` now catches network and non-JSON errors as typed codes (`registration_network_error`, `registration_bad_response`); `src/pages/ClientSignup.jsx` maps every backend error code to a friendly inline message via `SIGNUP_ERRORS` and guards against submit with a missing `joinToken` on scoped links.
- **Release registration-IP reservation on rejection (APPLIED).** Migration `20260919000003_release_ip_reservation_on_rejection.sql` (APPLIED to the remote project) adds the SECURITY DEFINER RPC `release_registration_link_ip_reservations_for_user(p_user_id uuid)` — the ONLY path that can clear a user's **finalized** `registration_link_ip_reservations` rows (the existing `release_registration_link_ip(uuid, inet)` only releases `status='reserved' AND user_id IS NULL`, i.e. the account-creation-failure path, and is a permanent no-op for finalized rows) — and redefines `reject_client_application(uuid, text)` so the canonical rejection workflow (calls from `PendingApplications.jsx` **Reject**) now invokes that release for `v_app.user_id` inside a best-effort `BEGIN/EXCEPTION` block (a release hiccup can never roll back the rejection), and extends the `client_application_rejected` audit metadata + RPC return with `ip_reservations_released`. Setting status to `'released'` evicts the row from the partial `uq_reg_link_ip` UNIQUE index (`WHERE status IN ('reserved','finalized')`), so the (link, id) pair is re-reservable by a legitimate re-registration. Gate: platform owner OR `auth.uid() = p_user_id`; idempotent (already-released rows untouched), resilient to no reservation, multiple rows across different links, missing/NULL user (`invalid_input`), unknown user (`released: 0`). Verified live end-to-end: register → finalized → reject releases (count 1) → same link+IP re-register succeeds → subsequent duplicate correctly `ip_taken`. Apply ONLY this file via `supabase db query --linked -f supabase/migrations/20260919000003_release_ip_reservation_on_rejection.sql` (never a bare `supabase db push` — it would also run the forbidden `20260917000001` cleanup).
- **Client activation gate + reconciliation (APPLIED — Active requires delivered plans).** Migration `20260920000002_client_activation_gate_and_reconciliation.sql` (APPLIED to the remote project) enforces the business rule that a client is only `Active` once the required plans are **delivered** — the exact semantics of the Forms Plan Delivery column (`get_forms_with_delivery`): an active non-archived Nutrition plan AND a non-template non-archived Workout plan. Root cause fixed: every creation/approval path (`approve_client_application` 20260905000010/20260903000005, `create_client_with_override`, the `clients.status` column DEFAULT) inserted `status='active'`, and only the event-driven `on_client_application_approved` AFTER-UPDATE trigger downgraded to `pending` — rows that never crossed that event (e.g. YBS-6895, stamped Active at approval with zero delivered plans) stayed wrongfully Active forever. Migration adds (1) shared authority `client_required_plans_delivered(uuid)`; (2) structural BACKSTOP trigger `trg_guard_client_activate` on `clients` (BEFORE INSERT OR UPDATE OF status) — INSERT always coerces to `pending`, UPDATE may reach `'active'` only when plans are delivered (`auto_activate_client`/`activate_client_package` pass since they verify first; direct SQL/future code can no longer shortcut); and (3) platform-owner/session RPC `reconcile_client_activation(p_apply=false default dry-run)` — reports the wrongly-active list first, then idempotently demotes them to `'pending'` (activated_at NULL) and parks their current active subscriptions to `'pending'` (mirroring the approval trigger) so `auto_activate_client` re-anchors the cycle at real activation; genuinely-delivered clients are never touched. Live reconciliation moved 5 wrongly-active clients to `pending` (YBS-6895, YBS-2698, YBS-4317, YBS-4902, YBS-5921; all `nutrition=false AND workout=false`), parked 5 subscriptions, one `client_activation_reconciled` audit row; afterwards `active` count = 19, all with delivered plans, re-run dry-run = 0. Frontend unchanged — Clients page already renders `Awaiting Activation` for non-active clients and the `Plan pending · N d overdue` hint stays consistent. Apply ONLY this file via `supabase db query --linked -f supabase/migrations/20260920000002_client_activation_gate_and_reconciliation.sql` (never a bare `supabase db push` — it would also run the forbidden `20260917000001` cleanup).
- **Authorized manual activation override (APPLIED — owner-forced activation).** Migration `20260920000004_manual_activation_override.sql` (APPLIED to the remote project) adds the onboarding-parity escape hatch for the plan-delivery rule above WITHOUT weakening the structural backstop. It adds `clients.activation_source TEXT CHECK (NULL | 'manual_override')` (durable marker, read by the Clients/ClientDetail UI), helper `manual_activation_override_active()` (reads transaction-scoped `ybs.manual_activation`), a redefined `guard_client_activate()` that promotes to `'active'` only while that flag is armed (and always stamps the marker), the SECURITY DEFINER RPC `override_activate_client(p_client_id UUID)` — the ONLY flag-writer: Platform Owner / Workspace Owner of the client's workspace / database-operator session gate, workspace-active check, idempotent, best-effort activation of the latest `pending` subscription (re-anchors like `auto_activate_client`; no pending cycle = lifecycle-only, billing untouched), sets status/activated_at/marker and appends a `client_manually_activated` audit row with nutrition/workout-delivered metadata — the read-only RPC `client_activation_readiness(p_client_id)` (`required_plans_delivered`/`nutrition_delivered`/`workout_delivered`) powering the UI warning, and a redefined `reconcile_client_activation()` that EXEMPTS `activation_source='manual_override'` from both dry-run and apply (an intentional override can never be swept away). Direct/unauthorized activation is still blocked: a raw `UPDATE … SET status='active'` without the flag is coerced to `pending` (verified live). Frontend: `SubscriptionsService.activateWithOverride` (`src/services/subscriptions.js`), `ClientsService.activationReadiness` (`src/services/clients.js`), and `ActivateClientButton` in `src/pages/ClientDetail.jsx` — now visible for ANY non-active client (`canEdit`), loads readiness on open, shows an amber "Activate Client (Manual Override)" warning + authorization checkbox (gated `canOverride` = `canManageClient`) when plans are pending, and the header shows a `Manual Override` badge when `activation_source==='manual_override'`. Verified live on Smoke Tester (SMOKE-T7, plans not delivered): gate rejects unauthenticated session, override via RPC → active + marker + audit (`subscription_activated:false` because no pending cycle), `reconcile_client_activation(false)`/`(true)` both skip it (`demoted_clients:0`), global count back to 19 all delivered after cleanup, exact original state restored (archived/active/NULL/NULL), audit rows kept. Apply ONLY this file via `supabase db query --linked -f supabase/migrations/20260920000004_manual_activation_override.sql` (never a bare `supabase db push` — it would also run the forbidden `20260917000001` cleanup).
- **Rejected-account lifecycle (APPLIED — re-registration after rejection).** Migration `20260920000001_rejected_account_lifecycle.sql` (APPLIED to the remote project) makes a rejected application's **phone AND email** eligible for a fresh registration-link registration again while preserving all history. Root cause was four occupied uniqueness layers: `auth.users.email` (`users_email_partial_key`), `auth.users.phone` (`users_phone_key`, set by `client-registration` on `createUser`), `auth.identities(provider_id=email, provider='email')`, and `profiles.email/phone` UNIQUE cols — plus `resolve_phone_identifier()` returning `found:true` for any profile row including `account_status='rejected'`. Lifecycle decision: rejection is TERMINAL (no un-reject path), so the rejected applicant's AUTH ACCOUNT is removed as part of rejection, freeing every uniqueness layer without weakening `resolve_phone_identifier` or the frontend "already registered" messages. History is preserved: `client_applications.user_id` FK is repointed to **nullable + ON DELETE SET NULL** (application row + applicant_name/phone/email snapshot + rejection_reason/reviewed_by/at survive), a `client_application_rejected` audit row is written BEFORE removal, and rejected applicants have no clients/timeline rows to lose. `reject_client_application()` is redefined with: platform-owner gate (unchanged), `approved`-application guard (`P0001: Approved applications cannot be rejected`), idempotent repeated rejection (no duplicate audit/delete), retro-fit removal of LEGACY rejected accounts (an already-rejected application whose account still lingers is removed on the next reject, audited with `retroactive_account_removal:true`), and a removal-eligibility guard (`platform_role='none'` AND `account_status IN ('pending_approval','rejected')`) so an active/suspended/deactivated or staff/owner profile is NEVER deleted. Verified live: same link+IP+phone+email re-register succeeds, same phone+different email and different phone+same email succeed, same identity re-registers through a DIFFERENT valid link, active-client phone/email still return `phone_taken`/`email_taken` 409, approved-application reject refused, pending/rejected no-user rows handled idempotently. Apply ONLY this file via `supabase db query --linked -f supabase/migrations/20260920000001_rejected_account_lifecycle.sql` (never a bare `supabase db push` — it would also run the forbidden `20260917000001` cleanup).

